services:
  app:
    extends:
      file: compose.common.yaml
      service: app
    build:
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    container_name: bank-rest-api

  db:
    extends:
      file: compose.common.yaml
      service: db
    container_name: bank-rest-api-db
